type User {
  id: ID!
  name: String!
  email: String!
  email_verified_at: DateTime
  password: String
  created_at: DateTime!
  updated_at: DateTime!
  tasks: [Task!]! @hasMany
  task_comments: [TaskComment!]! @hasMany
}
